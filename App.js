import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

export const App = () => {
  return (
    <View>
      <Text>Todo</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
